<%= render 'shared/page_title', title: "Chef #{@user.name}'s profile" %>

<div class="row">
  <div class="col-md-4 col-md-offset-4 center">
    <%= gravatar_for @user, size: 150 %>
  </div>
</div>

<h4 align="center"><%= @user.name %>'s recipes</h4>
<%= render "recipes/recipe", obj: @user.recipes %>        <!-- @user.recipes because we want to show up the recipes of this user that we click on -->
